<p id="notice"><%= notice %></p>

<%= link_to 'Back to Friend list', friends_path, class:"btn btn-dark" %>

<br/><br/><br/>

    <div class="p-5 mb-4 bg-light rounded-3">
        <div class="container-fluid py-5">
            <h1 class="display-5 fw-bold"><%= @friend.first_name %> <%= @friend.last_name %></h1>
            <p class="col-md-8 fs-4">
             ✉ <%= @friend.email %> |
             📲 <%= @friend.phone %> |
             tw: <%= @friend.twitter %></p>
            <br/>
            <%= link_to 'Edit Friend', edit_friend_path(@friend), class:"btn btn-secondary" %>
            <%= link_to 'Delete friend', @friend, method: :delete, data: { confirm: 'Delete this friend from your list?' }, class:"btn btn-danger btn" %>
        </div>
    </div>